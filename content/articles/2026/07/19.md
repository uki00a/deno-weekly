---
title: 2026/07/13〜2026/07/19の最新情報
tags:
  - Deno
categories:
  - news
date: 2026-07-19
description: >
  Deno v2.9.3 (`deno install` - `--no-save`/`--save-optional`オプションがサポート, `deno compile --target aarch64-pc-windows-msvc`, `crypto.randomUUID()`/`fs.cp()`/`StatementSync#all()`のパフォーマンスが改善, など), `deno lint` - graph-aware (cross-file) lint rules — prototype / RFC
---

## Deno v2.9.3

[Deno v2.9.3](https://github.com/denoland/deno/releases/tag/v2.9.3)がリリースされています。

### `deno add`/`deno install`

#### `--no-save`/`--save-optional`

`--no-save`及び`--save-optional`オプションが追加されています ([#36039](https://github.com/denoland/deno/pull/36039))

`--no-save`オプションが指定されると、`package.json`及び`deno.json`は更新せずに、指定されたパッケージのインストール及び`deno.lock`の更新のみが行われます。

`--save-optional`オプションが指定されると、指定されたパッケージをインストールしつつ、 `package.json`の `optionalDependencies`に該当パッケージが追加されます。

#### `--min-dep-age`

`--min-dep-age`オプションが追加されています ([#35914](https://github.com/denoland/deno/pull/35914))

実体は[`--minimum-dependency-age`]({{< ref "articles/2025/11/02.md" >}})オプションへの _alias_ です。

### `deno compile`

#### `--target aarch64-pc-windows-msvc`

`deno compile`コマンドで`--target aarch64-pc-windows-msvc`オプションがサポートされています ([#36004](https://github.com/denoland/deno/pull/36004))

[Deno v2.6.8]({{< ref "articles/2026/02/08.md" >}})にてWindows ARM64のサポートが追加されていたものの、`--target`オプションではサポートされていなかったようです。

### `Deno.createHttpClient()`

`Deno.createHttpClient()`に`http2MaxHeaderListSize`オプションが追加されています ([#33194](https://github.com/denoland/deno/pull/33194))

HTTP/2の`SETTINGS_MAX_HEADER_LIST_SIZE`に相当します。

### セキュリティ

#### BYONM

BYONM (`"nodeModulesDir": "manual"`) の有効化時に`require()`に`node_modules/../path/to/file.js`のような形式のパスが指定されると、意図せず`--allow-read`の要求が迂回されてしまう問題が修正されています ([#35882](https://github.com/denoland/deno/pull/35882))

### パフォーマンス改善

#### `crypto.randomUUID()`

`crypto.randomUUID()`のパフォーマンスが改善されています ([#35953](https://github.com/denoland/deno/pull/35953))。

初回の呼び出し時にあらかじめ128個分のUUIDを生成してプールしておき、以降の呼び出しにおいてはプールされたUUIDを返却することで、オーバーヘッドが削減されているようです。プールされているUUIDが尽きたら、再度、UUIDの生成が行われます。

ベンチマークによるとおおよそ2倍近くまでスループットが改善されているようです。

#### `node:fs`

`cp()`において、[一定条件](https://github.com/denoland/deno/blob/c71e43af07c3392c788286e20481998f6004b68d/ext/node/polyfills/_fs/cp/cp.ts#L168-L176)を満たす場合のパフォーマンスが最適化されています ([#35856](https://github.com/denoland/deno/pull/35856))

特に`recursive`オプションが有効化された際の振る舞いが改善されており、macOSにおいて、可能な場合は`clonefile()`の使用を試みることによって大幅なパフォーマンス改善が試みられているようです。

#### `node:sqlite`

`StatementSync#all()`パフォーマンスが改善されています ([#35863](https://github.com/denoland/deno/pull/35863))。行数が多い場合のパフォーマンスがNode.jsと同等以上にまで改善されているようです。

## `deno lint` - graph-aware (cross-file) lint rules — prototype / RFC

まだマージはされていませんが、PoCとして`deno lint`の[プラグインシステム]({{< ref "articles/deno/v2.2.md" >}})で複数のモジュールにまたがったLintルールを実装するための仕組みの開発が行われているようです。

- [feat(lint): graph-aware (cross-file) lint rules — prototype / RFC #35986](https://github.com/denoland/deno/pull/35986)

現時点 ([2f4e00e](https://github.com/denoland/deno/commit/2f4e00ebfd7f5cfe76982edaf9a31aa31b3863c7)) での実装としては、`Deno.lint.Rule`に`createGraphRule()`フックが追加されており、`ctx`引数の`.graph`プロパティーからアプリケーションを構成する各モジュールの情報を参照できる想定のようです。
