---
title: 2026/05/04〜2025/05/10の最新情報
tags:
  - Deno
categories:
  - news
date: 2026-05-10
description: >
  直近のDenoの変更について (`--node-modules-linker=hoisted`, `Deno.test()`で`timeout`オプションがサポート)
---

## Denoの直近の変更について

[先週]({{< ref "articles/2026/05/03.md" >}})に続いていくつかの新機能がmainブランチにマージされているため、紹介いたします。

### `--node-modules-linker=hoisted`

`--node-modules-linker`オプションが追加されています ([#32788](https://github.com/denoland/deno/pull/32788))。

現状ではこのオプションには`hoisted`または`isolated`のいずれかの値の指定がサポートされています。デフォルト値は`isolated`で、`isolated`が指定されている際は、今まで通り、pnpmなどと同様のレイアウトで`node_modules`が作成されます。

それに対して、`hoisted` (`--node-modules-linker=hoisted`) を指定すると、Denoがnpmと同様のレイアウトで`node_modules`を作成してくれます (`--node-modules-dir=manual`との併用が必要です)

これによりnpmを採用したプロジェクトとの互換性が改善されそうです。

### `Deno.test()`に`timeout`オプションが追加

`Deno.test()`に`timeout`オプションが追加されています ([#33815](https://github.com/denoland/deno/pull/33815))

ミリ秒単位でテストケースごとにタイムアウトを設定できます。

```javascript
Deno.test({
  name: "doSomethingAsync",
  timeout: 5_000,
  fn: async () => {
    const result = await doSomethingAsync();
    assert(result === 123);
  },
});
```
