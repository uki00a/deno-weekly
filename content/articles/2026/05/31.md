---
title: 2026/05/25〜2025/05/31の最新情報
tags:
  - Deno
  - deno_std
categories:
  - news
date: 2026-05-31
description: >
  Deno v2.8.1 (`deno bundle` - `package.json`の`sideEffects`フィールドがサポート, `package.json`でオブジェクト形式の`browser`フィールドがサポート, `deno pack` - `@deno/shim-deno`の自動注入機能が削除, `fetch()`/`WebSocket` - `--deny-net`使用時の振る舞いの変更, `--preload npm:<package>`がサポート, `node:module` - `enableCompileCache()`などがRevert, `node:process` - `loadEnvFile()`が`--allow-env`も要求するように挙動が変更, など), `deno_std`のリリース (`@std/async@1.4.0` - `@std/async/unstable-debounce`と`@std/async/unstable-lazy`が安定化, @std/collections@1.2.0 - `@std/collections/unstable-interleave`の安定化及び`Iterable`オブジェクトのサポート, `@std/data-structures@1.1.0` - `MultiMap`及び`IndexedHeap`が追加, `@std/data-structures/unstable-deque`が安定化, `@std/html@1.0.7` - `@std/html/unstable-html`が追加, `@std/http@1.1.1` - `routeRadix()`及びRFC 9421の実装が追加, `@std/streams@1.1.1` - `BatchStream`が追加, など)
---

## Deno v2.8.1

[Deno v2.8.1](https://github.com/denoland/deno/releases/tag/v2.8.1)がリリースされています。

### `deno bundle`

`package.json`の`sideEffects`フィールドがサポートされています ([#34406](https://github.com/denoland/deno/pull/34406))。`sideEffects`フィールドの設定値を元に Tree Shaking が行われるようです。

また、`--platform browser`向けに`package.json`でオブジェクト形式の`browser`フィールドがサポートされています ([#34407](https://github.com/denoland/deno/pull/34407))

### `deno pack`

[`deno pack`]({{< ref "articles/deno/v2.8.md" >}})コマンドにおける[`@deno/shim-deno`](https://github.com/denoland/node_shims)の自動注入機能が削除されています ([#34411](https://github.com/denoland/deno/pull/34411))

この変更に合わせて`--no-deno-shim`オプションも削除されています。

背景としては、`@deno/shim-deno`パッケージはしばらくメンテナンスされていない状況であったことが理由のようです。

### Web API

#### `fetch()`/`WebSocket`

`--deny-net`が指定された際に、`fetch()`及び`WebSocket`において、名前解決後のIPアドレスが考慮されるように挙動が変更されています ([#34236](https://github.com/denoland/deno/pull/34236))

`fetch()`/`WebSocket`に指定されたドメインの名前解決結果が`--deny-net`にマッチする場合、アクセスが拒否されます。

### `CompressionStream`

[Deno v2.8.0]({{< ref "articles/deno/v2.8.md" >}})において`new CompressionStream("brotli")`及び`new DecompressionStream("brotli")`の型チェックが失敗する問題が修正されています ([#34349](https://github.com/denoland/deno/pull/34349))

### Node.js 互換性の改善

#### `--preload npm:<package>`

[`--preload`]({{< ref "articles/deno/v2.4.md" >}})オプション ([`--import`]({{< ref "articles/deno/v2.4.md" >}})) でnpmパッケージの読み込みがサポートされています ([#34346](https://github.com/denoland/deno/pull/34346))

#### `node:module`

[Deno v2.8.0]({{< ref "articles/deno/v2.8.md" >}})にて導入されていた`enableCompileCache()`/`flushCompileCache()`/`getCompileCacheDir()`の追加が Revert されています ([#34348](https://github.com/denoland/deno/pull/34348))

Deno本体ではすでに[V8 code cache]({{< ref "articles/2024/04/28.md" >}})が実装されていることに加え、この変更によって意図せず`--allow-env`が要求されてしまう副作用があったことが理由のようです。

#### `node:process`

[`loadEnvFile()`]({{< ref "articles/2025/04/13.md" >}})が`--allow-read`だけでなく`--allow-env`も要求するように挙動が変更されています ([#34350](https://github.com/denoland/deno/pull/34350))

#### `node:tls`

`createServer()`で`pfx`オプションがサポートされています ([#34383](https://github.com/denoland/deno/pull/34383))

また、`createSecureContext()`における配列形式の`cert`/`key`/`pfx`オプションがサポートされています ([#34379](https://github.com/denoland/deno/pull/34379))

#### `node:util`

`getSystemErrorMap()`が実装されています ([#34372](https://github.com/denoland/deno/pull/34372))

## `deno_std`のリリース

`deno_std`のリリースが行われています ([release-2026.05.26](https://github.com/denoland/std/releases/tag/release-2026.05.26))

### `@std/async@1.4.0`

[@std/async@1.4.0](https://jsr.io/@std/async@1.4.0)がリリースされています。

#### `@std/async/unstable-debounce`

`@std/async/unstable-debounce`で提供されていた`debounce()`への`AbortSignal`のサポートが`@std/async/debounce`へマージされました ([#7096](https://github.com/denoland/std/pull/7096))

今後は`@std/async/debounce`においても`AbortSignal`サポートが利用できます。

#### `@std/async/unstable-lazy`

`@std/async/unstable-lazy`が安定化されました ([#7091](https://github.com/denoland/std/pull/7091))

今後は`@std/async/lazy`から利用できます。

#### `@std/async/unstable-channel`

`Channel`に対して以下の変更が実施されています:

- `receive()`及び`send()`メソッドに`signal`オプション (`AbortSignal`) が追加 ([#7086](https://github.com/denoland/std/pull/7086))
- `toReadableStream()`メソッドが追加 ([#7086](https://github.com/denoland/std/pull/7086))
- `constructor`が`ChannelOptions`オブジェクトを第1引数として受け取るように変更 ([#7106](https://github.com/denoland/std/pull/7106))

### `@std/cli@1.0.30`

[@std/cli@1.0.30](https://jsr.io/@std/cli@1.0.30)がリリースされています。

#### `@std/cli/unstable-progress-bar`

`ProgressBar`のオプションに関する以下の変更が実施されています:

- `refreshMilliseconds`オプションが`interval`にリネームされています ([#7094](https://github.com/denoland/std/pull/7094))
- `formatter`オプションに渡される`ProgressBarFormatter`オブジェクトの`styledData`プロパティーが関数から _getter_ に変更されています ([#7095](https://github.com/denoland/std/pull/7095))

### `@std/collections@1.2.0`

[@std/collections@1.2.0](https://jsr.io/@std/collections@1.2.0)がリリースされています。

#### `@std/collections/unstable-interleave`

`@std/collections/unstable-interleave`が安定化されています ([#7115](https://github.com/denoland/std/pull/7115))。今後は`@std/collections/interleave` から利用できます。

また、`interleave()`に`Iterable`オブジェクトがサポートも追加されています ([#7088](https://github.com/denoland/std/pull/7088))

### `@std/data-structures@1.1.0`

[@std/data-structures@1.1.0](https://jsr.io/@std/data-structures@1.1.0)がリリースされています。

#### 新規API

以下のモジュールが追加されています:

- `@std/data-structures/unstable-multimap`: `MultiMap` ([#7100](https://github.com/denoland/std/pull/7100))
- `@std/data-structures/unstable-indexed-heap`: `IndexedHeap` ([#7076](https://github.com/denoland/std/pull/7076))

#### `@std/data-structures/unstable-deque`

`@std/data-structures/unstable-deque`が安定化されています ([#7092](https://github.com/denoland/std/pull/7092))

今後は`@std/data-structures/deque`から利用できます。

#### `@std/data-structures/unstable-rolling-counter`

`RollingCounter`に以下のメソッドが追加されています:

- `at()`及び`toArray()` ([#7102](https://github.com/denoland/std/pull/7102))
- `toJSON()`及び`RollingCounter.from()` ([#7074](https://github.com/denoland/std/pull/7074))

### `@std/html@1.0.7`

[@std/html@1.0.7](https://jsr.io/@std/html@1.0.7)がリリースされています。

#### `@std/html/unstable-html`

新規モジュールとして`@std/html/unstable-html`が追加されています ([#7130](https://github.com/denoland/std/pull/7130))

`html()`関数が提供されており、_Tagged template_ によってHTMLを作成できます:

```jsx
html`<div>foo</div>`;
```

この関数は`deno fmt`によって[Tagged template内のHTMLがフォーマット]({{< ref "articles/deno/v2.3.md" >}})されるようにすることを目的に導入されたようで、現時点では埋め込む値に対するエスケープなどは実施されないため注意が必要そうです。

### `@std/http@1.1.1`

[@std/http@1.1.1](https://jsr.io/@std/http@1.1.1)がリリースされています。

#### `@std/http/unstable-route`

`routeRadix()`が追加されています ([#7075](https://github.com/denoland/std/pull/7075))。Fastifyなどと同様にRadix treeベースのルーター実装が提供されます。

この変更に伴い、既存の[`route()`]({{< ref "articles/2024/09/15.md" >}})は`routeLinear()`にリネームされています。

#### `@std/http/unstable-message-signatures`

`@std/http/unstable-message-signatures`が追加されています ([#7039](https://github.com/denoland/std/pull/7039))

[RFC 9421](https://datatracker.ietf.org/doc/html/rfc9421)の実装が提供されます。

### `@std/path@1.1.5`

[@std/path@1.1.5](https://jsr.io/@std/path@1.1.5)がリリースされています。

`@std/path`がNode.jsで動作するように内部実装が調整されています ([#7103](https://github.com/denoland/std/pull/7103))

### `@std/streams@1.1.1`

[@std/streams@1.1.1](https://jsr.io/@std/streams@1.1.1)がリリースされています。

#### `@std/streams/unstable-batch-stream`

新規モジュールとして`@std/streams/unstable-batch-stream`が追加されています ([#7110](https://github.com/denoland/std/pull/7110))

`BatchStream`が提供されており、コンストラクタで指定されたサイズが満たされるまでチャンクが内部でバッファリングされます。

### `@std/testing@1.0.19`

[@std/testing@1.0.19](https://jsr.io/@std/testing@1.0.19)がリリースされています。

[Deno v2.8.0]({{< ref "articles/deno/v2.8.md" >}})におけるタイマーAPIの変更に伴い、型定義が調整されています ([#7127](https://github.com/denoland/std/pull/7127))
