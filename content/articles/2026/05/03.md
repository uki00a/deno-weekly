---
title: 2026/04/27〜2026/05/03の最新情報
tags:
  - Astro
  - dax
  - Deno
  - Deno Deploy
  - fresh
  - Next.js
  - Nitro
  - Nuxt
  - Remix
  - Svelte
  - Vite
categories:
  - news
date: 2026-05-03
description: >
  直近のDenoの変更について (`deno compile` - フレームワークの検出機能が追加, `catalog:`プロトコルのサポート, `deno transpile`, `OffscreenCanvas`), Deno v2.7.14 (`deno upgrade` - bsdiffによる差分アップデート, `deno x` - jsr及び`https:`モジュール向けに`--unstable-*`オプションがサポート, macOSにおけるファイルシステム関連のパーミッションの挙動の変更, `structuredClone()`で`DataCloneError`を`throw`する変更, `CacheStorage`の`keys()`/`match()`メソッドの追加, `node:fs` - `Utf8Stream`や`mkdtempDisposable()`が実装, `node:cluster` - `Worker`や`fork()`などが実装, など), dax v0.46.0
---

## 直近のDenoの変更について

直近でDenoのmainブランチにいくつかの機能がマージされているため紹介します。

**⚠️ これらの機能はまだ正式にはリリースされていないため、今後、使用方法などが変更される可能性があります**

### `deno compile` - フレームワークの検出機能

`deno compile`コマンドにフレームワークの検出機能が追加されています:

- [feat: framework detection for deno compile #33164](https://github.com/denoland/deno/pull/33164)

`deno compile`の引数としてディレクトリが指定された場合、指定されたディレクトリ内に存在する設定ファイル (例: `next.config.js`)などに基づいてDenoが自動的にプロジェクトで使用されているフレームワークを検出し、必要に応じて該当フレームワーク向けのビルドコマンドを実行した上で、最終的に実行可能ファイルを作成してくれます。

現状、以下のフレームワークがサポートされているようです:

1. Next.js
2. Fresh
3. Astro
4. Nuxt
5. SvelteKit
6. Remix
7. SolidStart
8. TanStack Start
9. Vite (SSR)

[先週]({{< ref "articles/2026/04/26.md" >}})に紹介した`deno desktop`コマンドにおいてフレームワークの検出機能の導入が検討されており、その一環の対応であると思われます。

### `catalog:`プロトコル

カタログ機能を実装するPRがマージされています:

- [feat(npm): add `catalog:` protocol for centralized dependency versions in workspaces
#32947](https://github.com/denoland/deno/pull/32947)

ワークスペースのルートにおける`deno.json`または`package.json`において`catalog`もしくは`catalogs`プロパティーを定義しておくことで、[pnpm](https://github.com/pnpm/pnpm.io/blob/1a990a10149c16860813215adf29ff0c45c2d82c/docs/catalogs.md)などと同様にワークスペースメンバーの各`package.json`においてカタログで定義されたパッケージのバージョンを`catalog:`形式で参照することができます。

### `deno transpile`

[昨年]({{< ref "articles/2025/05/25.md" >}})に提案されていた`deno transpile`コマンドが実装されています:

- [feat: add `deno transpile` subcommand #32691](https://github.com/denoland/deno/pull/32691)

TypeScriptファイルをJavaScriptファイルにトランスパイルするためのコマンドで、`--declaration`オプションを指定することで`.d.ts`ファイルの生成も行えるようです。

現在はクローズされていますが、以前にDenoからnpmパッケージの公開を行えるようにするための[deno publish --npm]({{< ref "articles/2025/06/22.md" >}})の実装が進められており、`deno transpile`コマンドの追加はその一環である可能性もありそうです。

### `OffscreenCanvas`

[OffscreenCanvas](https://developer.mozilla.org/en-US/docs/Web/API/OffscreenCanvas)が実装されています:

- [feat: OffscreenCanvas #29357](https://github.com/denoland/deno/pull/29357)

この対応に合わせて`Deno.UnsafeWindowSurface`の`getContext()`メソッドにおける`"bitmaprenderer"`のサポートや`height`/`width`プロパティーの追加なども行われています。

## Deno v2.7.14

[Deno v2.7.14](https://github.com/denoland/deno/releases/tag/v2.7.14)がリリースされています。

### `deno upgrade`

[bsdiff](https://github.com/space-wizards/bsdiff-rs)を活用したアップデートの効率化が行われています ([#33274](https://github.com/denoland/deno/pull/33274))

今回のリリースから[GitHub Release](https://github.com/denoland/deno/releases/tag/v2.7.14)に`*.bsdiff`ファイルがアップロードされており、これをダウンロードすることで差分アップデートが実現されるようです。

本挙動は`--no-delta`オプションにより無効化できるようです。

### `deno x`

[`deno x`]({{< ref "articles/deno/v2.6.md" >}})コマンドでjsrパッケージや`https:`形式のモジュールを実行した際に、引数の`--unstable-*`オプションが無視されてしまう問題が修正されています ([#33450](https://github.com/denoland/deno/pull/33450))

### `deno deploy`

ワークスペースのルートで[`deno deploy`]({{< ref "articles/2025/05/25.md" >}})を実行すると、各メンバーがデプロイ対象から除外されてしまう問題が修正されています ([#33562](https://github.com/denoland/deno/pull/33562))

### Permissions

[Deno v2.7.10]({{< ref "articles/2026/04/05.md" >}})におけるWindows向けの修正に続いて、macOSにおいても`--allow-read`などのファイルシステム関連のパーミッションオプションが大文字や小文字の違いなどを考慮せずに権限チェックを行うように挙動が変更されています ([#33451](https://github.com/denoland/deno/pull/33451))

macOSのAPFSの挙動に合わせた変更のようです。

### Web API

#### `structuredClone()`

シリアライズに対応していないオブジェクトを`structuredClone()`に渡した際に`DataCloneError`が`throw`されるように挙動が修正されています ([#33465](https://github.com/denoland/deno/pull/33465), [#33491](https://github.com/denoland/deno/pull/33491))

#### Cache API

[`CacheStorage`](https://developer.mozilla.org/en-US/docs/Web/API/CacheStorage)の`keys()`及び`match()`メソッドが実装されています ([#33275](https://github.com/denoland/deno/pull/33275))

#### `MessageEvent`

`MessageEvent`の生成時に`source`オプションを指定した際に、生成された`MessageEvent`の`source`プロパティーが設定されない問題が修正されています ([#33500](https://github.com/denoland/deno/pull/33500))

#### `ReadableStreamBYOBRequest`

`ReadableStreamBYOBRequest`の`view`プロパティーの型が`ArrayBufferView`から`Uint8Array<ArrayBuffer>`に変更されています ([#33477](https://github.com/denoland/deno/pull/33477))

直近のSpec変更 ([whatwg/streams#1367](https://github.com/whatwg/streams/pull/1367)) に合わせた対応のようです。

### Node.js互換性の改善

#### `node:fs`

下記APIが実装されています:

- `Utf8Stream` ([#33485](https://github.com/denoland/deno/pull/33485))
- `mkdtempDisposableSync()`/`mkdtempDisposable()` ([#33533](https://github.com/denoland/deno/pull/33533))
- `watch()`の`ignore`オプション ([#33610](https://github.com/denoland/deno/pull/33610))
- `StatWatcher`の`ref()`/`unref()`メソッド ([#33408](https://github.com/denoland/deno/pull/33408))
- `stat()`及び`lstat()`の`throwIfNoEntry`オプション ([#33436](https://github.com/denoland/deno/pull/33436))

#### `node:net`

下記APIが実装されています:

- `Server#listen()`の`reusePort`オプション ([#33416](https://github.com/denoland/deno/pull/33416))
- `createServer()`及び`createConnection()`の`blockList`オプション ([#33417](https://github.com/denoland/deno/pull/33417), [#33414](https://github.com/denoland/deno/pull/33414))
- `createConnection()`の`signal`オプション ([#33414](https://github.com/denoland/deno/pull/33414))
- `Server#[Symbol.asyncDispose]()` ([#33417](https://github.com/denoland/deno/pull/33417))
- `BlockList`の`toJSON()`/`fromJSON()`メソッド, `rules`プロパティー, 及び`BlockList.isBlockList()` ([#33445](https://github.com/denoland/deno/pull/33445))
- `Socket#resetAndDestroy()` ([#33415](https://github.com/denoland/deno/pull/33415))

#### `node:http2`

様々なバグ修正に加え、下記新規APIが実装されています:

- `ServerHttp2Stream#pushStream()` ([#33516](https://github.com/denoland/deno/pull/33516))
- `createSecureServer()`の`ALPNCallback`オプション ([#33521](https://github.com/denoland/deno/pull/33521))
- `createServer()`の`strictFieldWhitespaceValidation`, `maxSessionInvalidFrames`及び`settings.maxHeaderListSize`オプション ([#33506](https://github.com/denoland/deno/pull/33506), [#33515](https://github.com/denoland/deno/pull/33515), [#33494](https://github.com/denoland/deno/pull/33494))

#### `node:dns`

下記APIが実装されています:

- `reverse()` ([#33579](https://github.com/denoland/deno/pull/33579))
- `Resolver`の`timeout`オプション及び`cancel()`メソッド ([#33580](https://github.com/denoland/deno/pull/33580))

#### `node:cluster`

下記APIの実装が行われています ([#33493](https://github.com/denoland/deno/pull/33493)):

- `Worker`
- `fork()`
- `setupPrimary()`
- `disconnect()`
- `isMaster`
- `isPrimary`
- `workers`
- `schedulingPolicy`
- `settings`
- `worker`
- `isWorker`

#### `node:child_process`

`spawn()`の`stdio`オプションにおける`Stream`オブジェクトの指定がサポートされています ([#33525](https://github.com/denoland/deno/pull/33525))

#### `node:perf_hooks`

`eventLoopUtilization()`が追加されています ([#33581](https://github.com/denoland/deno/pull/33581))

#### `node:process`

`process.debugPort`が実装されています ([#33403](https://github.com/denoland/deno/pull/33403))

#### `node:timers`

`Timeout`及び`Immediate`で`[Symbol.dispose]()`メソッドが実装されています([#33528](https://github.com/denoland/deno/pull/33528))

#### Node-API

`napi_async_init()`及び`napi_async_destroy()`が実装されています ([#33282](https://github.com/denoland/deno/pull/33282))

## dax v0.46.0

[dax v0.46.0](https://github.com/dsherret/dax/releases/tag/0.46.0)がリリースされています。

### 破壊的変更

#### `Deno.*`から`node:*`への移行

daxの内部実装におけるファイルシステム関連の操作などにおいて`Deno.*` APIではなく`node:fs`や`node:process`などを使用するように変更されています ([#364](https://github.com/dsherret/dax/pull/364))。これにより各プラットフォーム間におけるdaxの互換性の改善が期待されるようです。

ユーザーへの影響として、エラー発生時に`Deno.errors.*`ではなくNode.js互換のエラーが発生するように挙動が変わります。

#### `set -e`

dax内部のシェルの実装が[@david/shell](https://github.com/dsherret/shell)パッケージとして独立されたようです。これに伴い、シェルにおいて`set -e`がデフォルトで有効化されているようです。以前までの振る舞いに戻したい場合は明示的に`set +e`を実行する必要があります。

#### `$.select()`

`$.select()`の戻り値が`Promise<number>`から`Promise<SelectionItem>`に変更されています。`SelectionItem`は`index`プロパティーと`value`プロパティーを提供します ([#374](https://github.com/dsherret/dax/pull/374))

### 新規API

新規APIとしてコマンドの出力内容を`AsyncIterableIterator<string>`として取り扱うための`linesIter()`メソッド ([dsherret/shell#3](https://github.com/dsherret/shell/pull/3)) やユーザーに対して警告を表示するための`$.alert()` ([#368](https://github.com/dsherret/dax/pull/368))、`beforeRequest()`や`beforeCommand()`などのライフサイクルフック ([#373](https://github.com/dsherret/dax/pull/373)) などが追加されています。
