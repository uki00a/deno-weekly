---
title: 2026/02/02〜2026/02/08の最新情報
tags:
  - Deno
  - jsr
  - Deno Deploy
  - dax
categories:
  - news
date: 2026-02-08
description: >
  Deno Deployの一般公開とDeno Sandboxのアナウンス, Deno Deploy is Generally Available, Deno v2.6.8 (Windows ARM64のサポート, `package.json`の`dependencies`において`jsr:`スキームがサポート, `node:sqlite` - `SQLTagStore`が実装), dax v0.45.0 (`set`/`shopt`のサポート, `failglob`がデフォルトで無効化, `?`文字の取り扱いの変更, など)
---

## Deno Deploy is Generally Available

Deno Deployの一般公開が発表されました。

- [Deno Deploy is Generally Available](https://deno.com/blog/deno-deploy-is-ga)

また、[Deno Sandbox]({{< ref "articles/2026/01/25.md" >}})についてもDeno公式ブログで正式に発表されています。

- [Introducing Deno Sandbox](https://deno.com/blog/introducing-deno-sandbox)

[Deno Sandboxのランディングページ](https://deno.com/deploy/sandbox)も公開されています。

## Deno v2.6.8

[Deno v2.6.8](https://github.com/denoland/deno/releases/tag/v2.6.8)がリリースされています。

### Windows ARM64のサポート

DenoのWindows ARM64へのサポートが追加されています ([#31917](https://github.com/denoland/deno/pull/31917))

[GitHubのRelease](https://github.com/denoland/deno/releases/tag/v2.6.8)でもビルド済みのバイナリーが追加されています。

### jsr

[Deno v2.6.7]({{< ref "articles/2026/02/01.md" >}})における`@jsr`スコープの取り扱いの変更に続き、`package.json`の`dependencies`において`jsr:`スキームがサポートされています ([#31938](https://github.com/denoland/deno/pull/31938))

例えば、`package.json`で以下のように定義されている場合、[`jsr:@std/math@0.0.0`](https://jsr.io/@std/math@0.0.0)がインストールされます。

```json
{
  "dependencies": {
    "@std/math": "jsr:0.0.0"
  }
}
```

### `node:sqlite`

`DatabaseSync#createTagStore()` 及び`SQLTagStore`が実装されています ([#31945](https://github.com/denoland/deno/pull/31945))

また、`DatabaseSync`で`defensive`オプションのサポートが追加されています ([#32004](https://github.com/denoland/deno/pull/32004))

### `Worker`

`Worker`の各種コールバック (`onerror`, `onmessage`など) の型定義が誤っていた問題が修正されています ([#31981](https://github.com/denoland/deno/pull/31981))

## dax v0.45.0

[dax v0.45.0](https://github.com/dsherret/dax/releases/tag/0.45.0)がリリースされています。

### `set`/`shopt`のサポート

[Deno v2.6.6]({{< ref "articles/2026/01/25.md" >}})における変更に追従して、`set`/`shopt`が実装されています ([#353](https://github.com/dsherret/dax/pull/353))

この追加に合わせて `failglob`オプションがデフォルトで無効化されています ([#355](https://github.com/dsherret/dax/pull/355))。以前までのバージョンの挙動に戻したい場合、`CommandBuilder#failglob()`や`shopt -s failglob`などを実行する必要があります。

### `?`の取り扱いの変更

今まで`?`はパターンにおいて任意の一文字を表現するためのワイルドカードとして機能していましたが、このバージョンから単なる`?`文字として扱われるよう挙動が変更されています ([#356](https://github.com/dsherret/dax/pull/356))

必要な場合は、`CommandBuilder#questionGlob()`によって今までのバージョンの挙動に戻すことが可能です。

### `AbortSignal`サポートの拡充

`CommandBuilder#signal()`で`AbortSignal`がサポートされています ([#357](https://github.com/dsherret/dax/pull/357))

また、`RequestBuilder#signal()`が追加されています ([#358](https://github.com/dsherret/dax/pull/358))
