---
title: 2026/02/16〜2026/02/22の最新情報
tags:
  - Deno
  - Deno Deploy
  - deno_std
  - Hono
categories:
  - news
date: 2026-02-22
description: >
  Deno v2.7のリリースタイミングについて, Deno v2.6.10 (`deno install --global --compile`, `SSLKEYLOGFILE`, `node:worker_threads` - 入出力の取り扱いの改善, `AbortSignal.any()`で作成した`AbortSignal`が意図せずGCによって回収されてしまう問題の修正, など), `deno_std`のリリース (`@std/xml`パッケージが追加, `@std/async@1.2.0` - `@std/async/unstable-{lazy,poll,all-keyed}`の追加など, `@std/cli@1.0.28` - `StaticLine`の追加など, `@std/expect@1.0.18` - スナップショットテストのサポート, など), DenoとVultrのパートナーシップについて, Hono v4.12.0
---

## Deno v2.7のリリースタイミングについて

Deno公式のXアカウントにて、[今週にDeno v2.7のリリースを計画している](https://x.com/deno_land/status/2023792409704820743)旨について発表されています。

直近でTemporal APIの安定化 ([#31928](https://github.com/denoland/deno/pull/31928)) や `package.json`における`overrides`のサポート ([#32073](https://github.com/denoland/deno/pull/32073)) などがマージされており、これらがv2.7にてリリースされるものと思われます。

## Deno v2.6.10

[Deno v2.6.10](https://github.com/denoland/deno/releases/tag/v2.6.10)がリリースされています。

### `deno install --global --compile`

`deno install --global`で`--compile`オプションがサポートされています ([#32046](https://github.com/denoland/deno/pull/32046))

指定されたエントリーポイントをインストールする際に`deno compile`を適用して実行可能ファイルを作成してくれます。

```shell
$ deno install --global --compile -ER npm:cowsay

$ cowsay hi
 ____
< hi >
 ----
        \   ^__^
         \  (oo)\_______
            (__)\       )\/\
                ||----w |
                ||     ||
```

### `deno install`

`deno install`/`deno add`/`deno remove`の実行時に、不要なnpmパッケージを`node_modules`から削除してくれるよう挙動が改善されています ([#32058](https://github.com/denoland/deno/pull/32058))

### `SSLKEYLOGFILE`

[`SSLKEYLOGFILE`](https://github.com/curl/curl/blob/8494012196474ee0541940140344cd24ed340301/docs/cmdline-opts/_ENVIRONMENT.md#sslkeylogfile-path)環境変数がサポートされています ([#31867](https://github.com/denoland/deno/pull/31867))

Wiresharkなどでトラフィックを解析するために、指定されたファイルに対してTLSのセッションキーを書き込んでくれます。

### `deno publish`

`COPYING`ファイルが[ライセンスファイル]({{< ref "articles/deno/v1.46.md" >}})として認識されるよう改善されています ([#32133](https://github.com/denoland/deno/pull/32133))

### `node:worker_threads`

`Worker.stdin`がサポートされています ([#32165](https://github.com/denoland/deno/pull/32165))。`Worker`スレッド内からは`process.stdin`経由で`Worker.stdin`に書き込まれた内容を読み込めます。

同様に`Worker`スレッド内から`process.stdout`または`process.stderr`に書き込まれた内容が`Worker.stdout`または`Worker.stderr`経由で読み込めるようにする改善が実施されています ([#32160](https://github.com/denoland/deno/pull/32160))

また、`Worker`で`execArgv`オプションのサポートが追加されています ([#32145](https://github.com/denoland/deno/pull/32145))

### `node:fs`

`openSync()`で`O_RDONLY | O_CREAT`の組み合わせが動作するよう改善されています ([#32152](https://github.com/denoland/deno/pull/32152))

また、`opendir()`などが返却する`Dirent`クラスにおいて`parentPath`プロパティーが設定されるよう改善されています ([#32178](https://github.com/denoland/deno/pull/32178))

### `node:buffer`

`File`クラスが`export`されています ([#32182](https://github.com/denoland/deno/pull/32182))

### `AbortSignal.any()`

`AbortSignal.any()`で作成した`AbortSignal`が意図せずGCによって回収されてしまうことがある問題が修正されています ([#32118](https://github.com/denoland/deno/pull/32118))

### `Deno.test()`

`Deno.test()`で定義したテストケース中でネストして`Deno.test()`が呼ばれた際はエラーが発生するよう挙動が改善されています ([#32184](https://github.com/denoland/deno/pull/32184))

テストケースをネストさせたい場合は`Deno.TestContext#step()`の使用が推奨されます。

## `deno_std`のリリース

`deno_std`のリリースが実施されています ([release-2026.02.20](https://github.com/denoland/std/releases/tag/release-2026.02.20))

### `@std/xml`

新規パッケージとして[@std/xml](https://jsr.io/@std/xml@0.1.0)が追加されています  ([#6981](https://github.com/denoland/std/pull/6981), [#6997](https://github.com/denoland/std/pull/6997))。`parse()`や`parseXmlStream()`, `stringify()`などのAPIが提供されています。

### `@std/async@1.2.0`

[@std/async@1.2.0](https://jsr.io/@std/async@1.2.0)がリリースされています。

#### `@std/async/unstable-lazy`

新規モジュールとして`@std/async/unstable-lazy`が追加されています ([#7007](https://github.com/denoland/std/pull/7007))

このモジュールでは`Lazy`クラスが提供されています。`Lazy#get()`が最初に呼び出された際に一度だけコンストラクタで提供された関数を実行し、その戻り値を内部でキャッシュすることで、以降のすべての`Lazy#get()`の呼び出しでキャッシュされた値が返却されます。

#### `@std/async/unstable-poll`

新規モジュールとして`@std/async/unstable-poll`が追加されています ([#6973](https://github.com/denoland/std/pull/6973))

このモジュールでは`poll()`が提供されており、第2引数で指定した関数 (`isDone`) が`true`を返却するまで、第1引数で指定された関数を`interval`オプションで指定された間隔ごとに繰り返し呼んでくれます。

#### `@std/async/unstable-all-keyed`

`@std/async/unstable-all-keyed`が追加されています ([#6959](https://github.com/denoland/std/pull/6959))

`allKeyed()`及び`allSettledKeyed()`が提供されており、それぞれ[proposal-await-dictionary](https://github.com/tc39/proposal-await-dictionary)を実装しています。

#### `@std/async/retry`

`@std/async/unstable-retry`で提供されていた`retry()`の`signal`オプションが安定化されました ([#6968](https://github.com/denoland/std/pull/6968))。今後は`@std/async/retry`から`signal`オプションが利用できます。これに伴い`@std/async/unstable-retry`は削除されています。

#### `@std/async/unstable-abortable`

新規モジュールとして`@std/async/unstable-abortable`が追加されています ([#6971](https://github.com/denoland/std/pull/6971))

既存の`@std/async/abortable`では第2引数に直接`AbortSignal`が指定されることが想定されていますが、`@std/async/unstable-abortable`では`signal`オプションによる`AbortSignal`の指定がサポートされています。

### `@std/cli@1.0.28`

[@std/cli@1.0.28](https://jsr.io/@std/cli@1.0.28)がリリースされています。

#### `@std/cli/unstable-static-line`

新規モジュールとして`@std/cli/unstable-static-line`が追加されています ([#6758](https://github.com/denoland/std/pull/6758))。`StaticLine`クラスが提供されており、このクラスによって書き込まれたテキストは`console.log()`などによって大量の出力が行われてもターミナルの上部もしくは下部に留まって表示され続けます。

```javascript
import { StaticLine } from "jsr:@std/cli@1.0.28/unstable-static-line";

const delay = (ms) => new Promise((ok) => setTimeout(ok, ms));
const staticLine = new StaticLine();
await staticLine.write("Hello Deno!");
await delay(1000);
await staticLine.write("Hello JavaScript!");
for (let i = 0; i < 50; i++) {
  console.info(i);
}
await delay(1000);
await staticLine.releaseLine();
```

#### `@std/cli/parse-args`

`parseArgs()`に`-v=`のように引数に空の値が指定された際に、該当のオプションに`true`ではなく空文字列が設定されるよう挙動が修正されています ([#6995](https://github.com/denoland/std/pull/6995))

また、`parseArgs()`でプロトタイプ汚染攻撃への対策が実施されています ([#6980](https://github.com/denoland/std/pull/6980))

### `@std/expect@1.0.18`

[@std/expect@1.0.18](https://jsr.io/@std/expect@1.0.18)がリリースされています。

#### スナップショットテストのサポート

`expect().toMatchSnapshot()`と`expect().toMatchInlineSnapshot()`が実装されています ([#7003](https://github.com/denoland/std/pull/7003))

本機能を利用するには、現時点では`expect.setState()`を事前に呼ぶ必要があるようです。

```javascript
import { expect } from "jsr:@std/expect@1.0.18";

Deno.test("doSomething", (t) => {
  expect.setState({
    currentTestName: t.name,
    testPath: import.meta.url,
  });
  const result = doSomething();
  expect(result).toMatchSnapshot();
});
```

スナップショットを更新したい場合は`@std/testing/snapshot`と同様に`--update`の指定が必要です:

```shell
$ deno test -RW -- --update
```

### `@std/http@1.0.25`

[@std/http@1.0.25](https://jsr.io/@std/http@1.0.25)がリリースされています。

#### `@std/http/unstable-cache-control`

新規モジュールとして`@std/http/unstable-cache-control`が追加されています ([#7005](https://github.com/denoland/std/pull/7005))。`parseCacheControl()`と`formatCacheControl()`が提供されており、`Cache-Control`ヘッダーの設定値の解析・生成がサポートされています。

#### `@std/http/unstable-structured-fields`

新規モジュールとして`@std/http/unstable-structured-fields`が追加されています ([#6963](https://github.com/denoland/std/pull/6963))。[RFC 9651](https://www.rfc-editor.org/rfc/rfc9651)の実装が提供されます。

### `@std/json@1.0.3`

[@std/json@1.0.3](https://jsr.io/@std/json@1.0.3)がリリースされています。

#### `@std/json/unstable-canonicalize`

新規モジュールとして`@std/json/unstable-canonicalize`が追加されています ([#6965](https://github.com/denoland/std/pull/6965))。[RFC 8785](https://www.rfc-editor.org/rfc/rfc8785)が実装されています。

### `@std/assert@1.0.19`

[@std/assert@1.0.19](https://jsr.io/@std/assert@1.0.19)がリリースされています。

#### `@std/assert/object-match`

`assertObjectMatch()`において`Date`オブジェクトが適切に比較されていなかった問題が修正されています ([#6988](https://github.com/denoland/std/pull/6988))

## DenoとVultrのパートナーシップについて

Denoが[Vultr Cloud Alliance](https://www.vultr.com/cloudalliance/)へ参加したことが発表されました。

- [Modern JavaScript and TypeScript Applications Built with Deno on Vultr](https://blogs.vultr.com/deno-cloud-alliance)

元々、Deno Deployの内部で[Vultr](https://www.vultr.com/)が使用されていたようで、その関係でパートナーシップが結ばれたようです。

また、Vultrの以下の公式ドキュメントにて[Vultr Serverless Inference](https://www.vultr.com/products/cloud-inference/)と[Deno Sandbox]({{< ref "articles/2026/01/25.md" >}})の連携例が紹介されています:

- [How to Utilize Deno Sandboxes With Vultr](https://docs.vultr.com/how-to-utilize-deno-sandboxes-with-vultr)

## Hono v4.12.0

[Hono v4.12.0](https://github.com/honojs/hono/releases/tag/v4.12.0)がリリースされています。

### `hono/ssg`

`redirectPlugin`が追加されています ([#4599](https://github.com/honojs/hono/pull/4599))。301などのステータスコードを返却したページが検出された場合、`Location`ヘッダーの設定値に基づいて[`http-equiv="refresh"`](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/meta/http-equiv)が設定された静的なHTMLページを生成してくれます。

### `hono/client`

`ApplyGlobalResponse`型が追加されています ([#4556](https://github.com/honojs/hono/pull/4556))。主にエラーレスポンスに関する型をクライアントが推論できるようにすることを目的に追加されたようです。

クライアントに`$path()`メソッドが追加されています ([#4636](https://github.com/honojs/hono/pull/4636))。`$url()`と異なり、パスを文字列で返却してくれます。

### `hono/basic-auth`

`onAuthSuccess`コールバックがサポートされています ([#4645](https://github.com/honojs/hono/pull/4645))。認証成功時に`Context`及びユーザー名を引数にコールバックが呼ばれます。

### `hono/trailing-slash`

`trimTrailingSlash()`及び`appendTrailingSlash()`に`alwaysRedirect`オプションが追加されています ([#4658](https://github.com/honojs/hono/pull/4658))。

例えば、`trimTrailingSlash()`で`alwaysRedirect: true`を設定した場合、末尾が`/`で終わる`GET`または`HEAD`リクエストを受信した場合、常に末尾の`/`を取り除いたパスへリダイレクトするようです。

### `hono/language`

[RFC 4647](https://datatracker.ietf.org/doc/html/rfc4647)が実装されています ([#4717](https://github.com/honojs/hono/pull/4717))。これにより、例えば`ja-JP`が`ja`にマッチしてくれるよう挙動が改善されるようです。

### `hono/router/trie-router`

`TrieRouter`の内部実装の改善により、パフォーマンスが1.5〜2倍近くまで高速化されたようです ([#4724](https://github.com/honojs/hono/pull/4724))
