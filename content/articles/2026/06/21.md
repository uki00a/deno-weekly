---
title: 2026/06/15〜2026/06/21の最新情報
tags:
  - Deno
categories:
  - news
date: 2026-06-21
description: >
  Denoの直近の変更について (`deno desktop`がマージ, `package-lock.json`/`pnpm-lock.yaml`から`deno.lock`への移行がサポート, CSS ファイルの`import` (`with { type: "css" }`), スナップショットテスティング)
---

## Denoの直近の変更について

直近でDeno 2.9向けと思われるPRがいくつかマージされているため紹介します。

### `deno desktop`コマンド

[以前]({{< ref "articles/2026/04/26.md" >}})に紹介した`deno desktop`を実装するPRがmainブランチにマージされています:

- [feat: `deno desktop` subcommand (denoland/deno#33441)](https://github.com/denoland/deno/pull/33441)

以下のいずれかのバックエンドを切り替えることができるようです:

- cef ([Chromium Embedded Framework](https://github.com/chromiumembedded/cef))
- webview
- raw

rawバックエンドはユーザーがWebベースのエンジン (cef / webview) を使わずに独自にレンダリングをカスタマイズしたい場合に使うことが想定されているようです。

また`--hmr`オプションによるHMRのサポート、`Deno.serve()`との統合、`bindings[name]()`によるDenoランタイムからバックエンドへの関数の公開、`alert()`/`confirm()`などによるネイティブポップアップの表示、`Notification`APIによる通知、`Deno.BrowserWindow`/`Deno.Tray`/`Deno.dock`/`Deno.MenuItem`/`Deno.autoUpdate()`などのAPI、[フレームワークの自動検出]({{< ref "articles/deno/v2.8.md" >}})などがサポートされているようです。

公式ドキュメントもすでに公開されており、Deno 2.9にてリリースされる予定のようです:

- [docs: add `deno desktop` reference page (denoland/docs#3085)](https://github.com/denoland/docs/pull/3085)

### `package-lock.json`/`pnpm-lock.yaml`/`yarn.lock` (Yarn v1)/`bun.lock`から`deno.lock`への移行がサポート

`deno.lock`が存在しない状態での`deno install`コマンドの実行時に、`package-lock.json`/`pnpm-lock.yaml`/`yarn.lock` (Yarn v1)/`bun.lock`のいずれかが検出されたら、それらの内容から`deno.lock`を自動生成してくれる機能が実装されています:

- [feat(install): seed deno.lock from package-lock.json #35330](https://github.com/denoland/deno/pull/35330)
- [feat(install): seed deno.lock from pnpm-lock.yaml #35346](https://github.com/denoland/deno/pull/35346)
- [feat(install): seed deno.lock from yarn.lock #35350](https://github.com/denoland/deno/pull/35350)
- [feat(install): seed deno.lock from bun.lock #35394](https://github.com/denoland/deno/pull/35394)

### CSS ファイルの`import` (`with { type: "css" }`)

`.css`ファイルの`import`に関する実験的サポートが追加されています:

- [feat(unstable): CSS module imports (with { type: "css" }) #35093](https://github.com/denoland/deno/pull/35093)

`with { type: "css" }`を指定することで、`.css`ファイルを[`CSSStyleSheet`](https://developer.mozilla.org/en-US/docs/Web/API/CSSStyleSheet)オブジェクトとして読み込むことができます:

```javascript
import stylesheet from "./styles.css" with { type: "css" };

console.assert(stylesheet instanceof CSSStyleSheet);
```

本機能を利用するためには[`--unstable-raw-imports`]({{< ref "articles/deno/v2.4.md" >}})の指定が必要です。

### スナップショットテスティング

スナップショットテスティングがサポートされています:

- [feat(test): built-in snapshot testing via t.assertSnapshot #35139](https://github.com/denoland/deno/pull/35139)

`Deno.TestContext#assertSnapshot()`というAPIが追加されており、デフォルトでは`__snapshots__/*.ts.snap`に`@std/testing/snapshot`と同様のフォーマットでスナップショットが保存され、`deno test`コマンドの`--update-snapshots`オプションによってスナップショットを更新できるようです。
